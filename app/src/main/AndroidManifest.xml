<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.rpgstatmanager">

    <application
        android:name=".RPGStatManagerApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".view.activity.CharacterActivity"></activity>
        <activity android:name=".view.activity.login.ForgottenPasswordActivity" />
        <activity android:name=".view.activity.AdventureCreatorActivity" />
        <activity android:name=".view.activity.character.AllMovesActivity" />
        <activity android:name=".view.activity.character.EmotionModifierActivity" />
        <activity android:name=".view.activity.character.MoveTypeActivity" />
        <activity android:name=".view.activity.CharacterChooserActivity" />
        <activity android:name=".view.activity.character.RaceActivity" />
        <activity android:name=".view.activity.character.PersonalityTypeActivity" />
        <activity android:name=".view.activity.character.ToolActivity" />
        <activity android:name=".view.activity.character.WeaponActivity" />
        <activity android:name=".view.activity.character.StatActivity" />
        <activity android:name=".view.activity.AdventureChooserActivity" />
        <activity
            android:name=".view.activity.login.LoginActivity"
            android:label="@string/title_activity_login" />
        <activity android:name=".view.activity.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>