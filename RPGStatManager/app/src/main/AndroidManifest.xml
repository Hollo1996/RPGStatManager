<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.rpgstatmanager">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".view.character.AllMovesActivity"></activity>
        <activity android:name=".view.character.EmotionActivity" />
        <activity android:name=".view.character.MoveBaseActivity" />
        <activity android:name=".view.character.CharacterChooserActivity" />
        <activity android:name=".view.character.RaceActivity" />
        <activity android:name=".view.character.PersionalityActivity" />
        <activity android:name=".view.character.ToolActivity" />
        <activity android:name=".view.character.WeaponActivity" />
        <activity android:name=".view.character.StatActivity" />
        <activity android:name=".view.AdventureChooserActivity" />
        <activity
            android:name=".ui.login.LoginActivity"
            android:label="@string/title_activity_login" />
        <activity android:name=".view.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>